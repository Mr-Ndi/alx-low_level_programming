#include<stdio.h>
/**
 * main - printing all alphabets in lower case
 * Return: Always 0.
 */
void prtint_alphabet(void)
{
char g;

for (l = 'a'; l <= 'z'; l++)
{
putchar(l);
}
putchar('\n');
return (0);
}
