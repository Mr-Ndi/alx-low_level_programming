#include<stdio.h>
/**
 * main cpn wabibonye ko comment doesn't matter
 * Return: always 0.
 */
int main(void)
{
int p;
int k;
for (p = 48; p <= 57; p++)
{
putchar(p);
}
for (k =97; k <= 102; k++)
{
putchar(k);
}
putchar('\n');
return (0);
}
